import AnimatedLog from '../assets/images/logo-animated.gif'

const Loader = () => {
    return (
        <div className="loader">
            <img src={AnimatedLog} alt="Apple site is Loading" className="log" />
        </div>
    )

}

export default Loader